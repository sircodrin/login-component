playmorphia {
    uri="${db.host}:${db.port}"
    database=${db.name}
    models="models"
    mongoClientFactory="dot.cpp.repository.mongodb.MyMongoClientFactory"
}

constraints {
    field {
        invalid="Invalid"
    }
}